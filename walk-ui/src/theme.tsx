import { createTheme } from '@fluentui/react';

export const azureTheme = createTheme({
  palette: {
    themePrimary: '#0078d4',
    white: '#ffffff',
    neutralLight: '#f3f2f1',
    neutralLighter: '#faf9f8',
  },
});
